"use client";

import React from "react";
import { BookingForm } from "@/app/admin/crm/components/BookingForm";

// Ruta pública para el formulario de registro / lead intake.
export default function BookingPublicPage() {
  return <BookingForm />;
}
